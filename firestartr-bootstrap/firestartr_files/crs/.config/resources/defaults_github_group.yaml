name: group_base
apiVersion: firestartr.dev/v1
kind: GroupClaim
defaultValues:
  context:
    backend:
      ref:
        kind: FirestartrProviderConfig
        name: firestartr-terraform-state
    provider:
      ref:
        kind: FirestartrProviderConfig
        name: github-app
