type: tfworkspaces
name: example-platform
tenants: [test-tenant]
envs: [test-env]
allowedClaims:
  - resourceTypes: 
      - az-vmss
    providers:
      - azure-provider-example
    backend: azure-backend-terraform
